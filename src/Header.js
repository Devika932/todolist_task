import React from 'react';

const Header = () => {
    return (
        <header>
            <h1 style={{color:"blue", padding:"0px"}}>TODO LIST</h1>
        </header>
    );
};

export default Header;